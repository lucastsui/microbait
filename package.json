{
  "name": "microbait",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "electron/main.js",
  "description": "Standalone app that briefs your X feed from Electron Chromium using Grok CLI or the OpenCode API.",
  "scripts": {
    "start": "electron .",
    "server": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:e2e": "node e2e/desk.mjs",
    "eval": "./eval.sh"
  },
  "engines": {
    "node": ">=20.11"
  },
  "devDependencies": {
    "electron": "^37.2.6",
    "playwright-core": "^1.55.0"
  }
}
