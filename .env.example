# SpaceXAI / xAI key. Live briefings use Grok with web_search + x_search.
# Without this key the site still runs on public RSS / Hacker News plus the local stripper.
XAI_API_KEY=

# Optional override. Default is grok-4.6
XAI_MODEL=grok-4.6

PORT=3847
